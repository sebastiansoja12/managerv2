package com.warehouse.routetracker.infrastructure.adapter.primary.dto;

public enum ShipmentStatusDto {
    PLANNED, CREATED, PREPARED, ACCEPTED, RETURN, ROUTE, REJECT, REROUTE, REDIRECT, SENT, DELIVERY, CANCELED
}
